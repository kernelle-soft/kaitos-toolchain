pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "go/**/*.go"
      run: bash -c '. shell/.shock/lib/lint.api.sh && lint_go {staged_files}'

    rustfmt:
      glob: "crates/**/*.rs"
      run: bash -c '. shell/.shock/lib/lint.api.sh && lint_rust'
