pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "go/**/*.go"
      run: source scripts/shared/lint_go.func.sh && lint_go {staged_files}

    rustfmt:
      glob: "crates/**/*.rs"
      run: source scripts/shared/lint_rust.func.sh && lint_rust
