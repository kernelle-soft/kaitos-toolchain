pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "go/**/*.go"
      run: bash -c '. scripts/shared/lint.api.sh && lint_go {staged_files}'

    rustfmt:
      glob: "crates/**/*.rs"
      run: bash -c '. scripts/shared/lint.api.sh && lint_rust'
