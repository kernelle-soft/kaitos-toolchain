name: Validate Release
on:
  workflow_call:

jobs:
  lint:
    uses: ./.github/workflows/_lint.yaml
    secrets: inherit
  
  coverage:
    uses: ./.github/workflows/_coverage.yaml
    secrets: inherit

